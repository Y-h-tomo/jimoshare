.field.item-sort
  = form_tag items_path, method: :get do
    = select_tag :prefecture_id,options_from_collection_for_select(Prefecture.all, :id, :name, params[:prefecture_id]),{prompt: '地域で絞り込み',class: 'form-control posts__select'}